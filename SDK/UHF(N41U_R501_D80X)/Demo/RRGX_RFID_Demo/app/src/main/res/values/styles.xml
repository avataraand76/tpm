<resources>


    <!-- Application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="OverflowMenuStyle"  parent="@android:style/Widget.PopupMenu">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">0dp</item>
        <item name="android:popupBackground">@drawable/ic_bg_popup</item>
        <!--        <item name="android:background">@mipmap/ic_bg_popup</item>-->
        <item name="android:dropDownVerticalOffset">-7dp</item>
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <item name="android:textColor">#1a1a1a</item>
        <!--<item name="android:dropDownListViewStyle">@style/AppDropDownListViewStyle</item>-->
    </style>

</resources>
